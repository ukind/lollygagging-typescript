(function () {
    var isAdmin = true;
    var userName = "Yogiswara";
    var message;
    if (isAdmin) {
        console.log("welcome! " + userName);
    }
    else {
        console.log("access denied");
    }
})();
