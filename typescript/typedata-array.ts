(function () {
  const ageList: number[] = [4, 8, 2, 6, 4];
  ageList.push(22);

  const listOfName: string[] = ["yogiswara", "utama"];
  listOfName.push("utamakan belajar");

  const listOfNameAndAge: Array<number | string> = [
    "tikus",
    "makan",
    34,
    "sabun",
  ];

  const anotherMixedValue: (boolean | number | string)[] = [
    true,
    "or",
    false,
    "doesnt matter",
  ];
})();
