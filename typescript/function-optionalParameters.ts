(function () {
  function greet(
    name: string,
    age: number,
    optimalParameterPlace?: string
  ): void {
    console.log(
      `hello ! ${name}, your are is ${number}. welcome to ${optimalParameterPlace}`
    );
  }
})();
